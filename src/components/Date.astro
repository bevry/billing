---
import type Daet from 'daet'
export interface Props {
	date: Daet
}
const { date } = Astro.props
const result = date.format('en', {
	year: 'numeric',
	month: 'long',
	day: 'numeric',
})
---

{result}
